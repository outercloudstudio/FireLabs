Install the extension and then add the `firework` compiler plugin to your build config.

```json
{
    "plugins": [
        [
            "comMojangRewrite",
            {
                "packName": "BridgeTest"
            }
        ],
        "firework"
    ]
}

```
